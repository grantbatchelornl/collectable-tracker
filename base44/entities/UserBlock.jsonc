{
  "name": "UserBlock",
  "type": "object",
  "properties": {
    "blocker_id": {
      "type": "string"
    },
    "blocked_id": {
      "type": "string"
    },
    "blocked_name": {
      "type": "string"
    }
  },
  "required": [
    "blocker_id",
    "blocked_id"
  ],
  "rls": {
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "read": {
      "$or": [
        {
          "data.blocker_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "super_admin"
          }
        }
      ]
    },
    "update": {
      "data.blocker_id": "{{user.id}}"
    },
    "delete": {
      "data.blocker_id": "{{user.id}}"
    }
  }
}